# 相机标定与视觉链路分析

## 1. 相机标定结果

### 标定参数（2026-04-26）

使用棋盘格 **9×6 内角点**（10×7 黑白方块），每个格子边长 **25mm**，采集约 15~20 张不同角度图像。

| 参数 | 值 |
|------|-----|
| **fx** | 689.58 |
| **fy** | 686.99 |
| **cx** | 982.87 |
| **cy** | 394.59 |
| **k1** | -0.140459 |
| **k2** | 0.270074 |
| **p1** | 0.000120 |
| **p2** | 0.003055 |
| **k3** | -0.395587 |
| **RMS 误差** | 0.9759 px |

已硬编码到 `host-rk3588/rga_npu.cpp`：

```cpp
static const cv::Mat CAMERA_MATRIX = (cv::Mat_<float>(3,3) <<
    689.58f, 0.0f,     982.87f,
    0.0f,    686.99f,  394.59f,
    0.0f,    0.0f,     1.0f);

static const cv::Mat DIST_COEFFS = (cv::Mat_<float>(1,5) <<
    -0.140459f, 0.270074f, 0.000120f, 0.003055f, -0.395587f);
```

### 标定方法

```bash
cd host-rk3588/scripts
python3 calibrate.py
```

操作步骤：
1. 脚本自动打开 `/dev/video21`
2. 实时检测棋盘格角点
3. 按 `[空格]` 保存当前帧（建议保存 15~20 张不同角度和距离）
4. 按 `[c]` 开始计算
5. 输出 `camera_matrix.npy` / `dist_coeffs.npy` 和文本版本

---

## 2. PnP 问题分析：镜像解跳变

### 现象

USB 摄像头替换后，正脸时 3D 框没有中间状态：人脸稍偏左 → 框偏左很多，人脸稍偏右 → 框直接跳到偏右很多。

### 根因

`solvePnP` 的 6 个 3D 关键点（双眼、鼻尖、嘴角、下巴）近似**共面**。当相机内参不准 + 3D 点过于共面时，迭代法容易在"真实解"和"镜像解"之间横跳。

| 条件 | MIPI 摄像头 (旧) | USB 摄像头 (新) |
|------|-----------------|----------------|
| fx | 1371 | 689.58 |
| 鼻子 Z 坐标 | -70（比眼睛深 10mm） | -90（比眼睛深 30mm） |
| 结果 | 稳定，不跳变 | `-70` 时跳变；`-90` 后稳定 |

### 结论

`-70` 本身没错，但在 fx 变化后让 3D 点更接近共面，放大了 PnP 歧义性。将鼻尖 Z 从 `-70` 改到 `-90` 增强了非共面性（鼻子深 30mm），消除了歧义。

### FACE_3D_POINTS 当前定义

```cpp
static const std::vector<cv::Point3f> FACE_3D_POINTS = {
    {-30.0f, -25.0f, -60.0f},   // 1: right eye
    {30.0f,  -25.0f, -60.0f},   // 2: left eye
    {0.0f,   -5.0f,  -90.0f},   // 0: nose
    {-25.0f, 20.0f,  -65.0f},   // 17: right mouth
    {25.0f,  20.0f,  -65.0f},   // 18: left mouth
    {0.0f,   50.0f,  -40.0f}    // 19: chin
};
```

单位：mm。建议后续若更换摄像头或模型，优先检查 PnP 稳定性。

---

## 3. YUYV vs MJPG 格式对比

### 摄像头状态

- **硬件**: Realtek USB Camera3 (`0bda:5858`)，原生支持 USB3.0
- **当前故障**: 固件错误导致被识别为 **USB2.0 设备**
- **影响**: YUYV 1920×1080 在 USB2.0 带宽下只能跑 **~5fps**

### 带宽与帧率

| 格式 | 单帧数据量 | USB3.0 传输时间 | USB2.0 实际帧率 |
|------|-----------|----------------|----------------|
| **YUYV 1080p** | 1920×1080×2 = **4.15 MB** | ~3~5 ms | **~5 fps**（带宽瓶颈） |
| **MJPG 1080p** | 压缩后 **~100~300 KB** | ~0.5~1 ms | **30 fps** 无压力 |

### Frame → PnP 延迟拆解（30fps 基准）

| 环节 | YUYV 路径 | MJPG 路径 | 差异 |
|------|----------|----------|------|
| USB 传输 | ~3~5 ms | ~0.5~1 ms | MJPG 省 ~3~4 ms |
| 格式转换 | RGA YUYV→NV12 **<1 ms** | mppjpegdec 硬件解码 **~5~10 ms** | MJPG 多 ~5~9 ms |
| NPU 推理 | ~10~15 ms | ~10~15 ms | 持平 |
| PnP + 绘制 | ~10~20 ms | ~10~20 ms | 持平 |
| 其他开销 | ~5~10 ms | ~5~10 ms | 持平 |
| **Frame→PnP 总计** | **~56~62 ms（实测）** | **~55~65 ms（预估）** | **基本持平** |

### 结论

1. **USB3.0 正常时**：YUYV 和 MJPG 的 Frame→PnP 延迟几乎一样（~56~62ms）。YUYV pipeline 更简单（RGA 一步格式转换），MJPG 多了 JPEG 解码环节，省下的 USB 传输时间被解码时间抵消。

2. **USB2.0 fallback 时（当前故障状态）**：
   - YUYV：只能 5fps，帧周期 200ms，总延迟 >200ms
   - MJPG：仍可达 30fps，Frame→PnP 仍保持 ~55~65ms
   - **这是 MJPG 的唯一优势：在 USB 带宽受限时维持高帧率**

3. **建议**：
   - 摄像头修好后（恢复 USB3.0），**继续使用 YUYV**，pipeline 更简洁
   - 仅在 USB2.0 fallback 或需要同时跑多路视频时，才考虑 MJPG

---

## 4. 视觉链路性能指标

| 指标 | 数值 |
|------|-----|
| 端到端延迟（Frame → PnP） | **~56~78 ms**，baseline ~58~62 ms |
| 帧周期（30fps） | ~33 ms |
| USB 传输 | ~3~5 ms (YUYV) / ~0.5~1 ms (MJPG) |
| RGA 转换 + NPU 推理 + 后处理 + PnP | ~15~25 ms |
| NPU 负载（YOLOv8-pose 640×640） | ~10~15 ms |

---

## 5. OneEuroFilter 参数

当前针对不同关键点使用两组参数：

| 类型 | 关键点 | mincutoff | beta | 说明 |
|------|--------|-----------|------|------|
| **弱滤波** | 眼睛、嘴角 (0,1,2,17,18,19) | 1.2 | 0.015 | 这些点易抖动，允许稍多平滑 |
| **强滤波** | 其他点 | 0.8 | 0.008 | 更紧致跟随，减少滞后 |

当摄像头帧率变化时，需要同步调整 `freq` 参数：
- 30fps：`freq = 30.0`（或 15.0，根据采样频率定义）
- 5fps：`freq = 5.0`

---

## 6. 手眼标定与正运动学标定（待完成）

这是坐标变换闭环的前置条件，详见 [`docs/architecture.md`](docs/architecture.md)。

简要步骤：
1. 打印 ArUco 标记贴在机械臂末端
2. 让机械臂处于 3~5 个已知关节角姿态
3. RK3588 拍图，OpenCV `cv::aruco::estimatePoseSingleMarkers()` 获取标记位姿
4. 用 `cv::calibrateHandEye()` 求解 `T_end←camera`
5. 同时验证正运动学：根据关节角计算的末端位置，与图像中标记的实际位置是否一致
